package timer;

public class Arithmetic {
	/*
	 * This is a class that can create an object.
	 * When you call the object it should have two 
	 * parameters, the parameters will be two real
	 * numbers.
	 * Arithmetic one = new Arithmetic(34.0, 20.6);
	 * 
	 * These two parameters must be assigned to 
	 * two private variables.
	 * 
	 * Then the class must contain 5 methods each
	 * method must return the result of addition, 
	 * subtraction, multiplication, division and modulo.
	 * 
	 * eg ...  one.add();
	 */
}
